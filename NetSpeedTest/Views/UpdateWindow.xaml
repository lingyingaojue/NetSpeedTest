<Window x:Class="NetSpeedTest.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" Width="520" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True"
        WindowStyle="ToolWindow"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource AccentBrush}" CornerRadius="10" Padding="14,5"
                HorizontalAlignment="Center" Margin="0,0,0,14">
            <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold"
                       Foreground="{DynamicResource BackgroundBrush}" />
        </Border>

        <TextBlock Grid.Row="1" Text="{DynamicResource Update_Title}" FontSize="12"
                   Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,6" />

        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12,10"
                MaxHeight="300">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="BodyText" FontSize="13" LineHeight="22"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           TextWrapping="Wrap" />
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="{DynamicResource Update_Download}" Width="90" FontSize="14"
                    Style="{StaticResource AccentButtonStyle}" Click="Download_Click" />
            <Button Content="{DynamicResource Update_Later}" Width="70" Margin="8,0,0,0"
                    Click="Later_Click" />
        </StackPanel>
    </Grid>
</Window>
