<UserControl x:Class="NetSpeedTest.Views.ProfileConfigPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:NetSpeedTest.Converters">

    <UserControl.Resources>
        <converters:NotNullConverter x:Key="NotNullConverter" />
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部：导入导出 -->
        <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource Profile_Title}" FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center" />
                <Button Grid.Column="1" Content="{DynamicResource Import}" Command="{Binding ImportConfigCommand}"
                        Width="60" Margin="0,0,8,0" />
                <Button Grid.Column="2" Content="{DynamicResource Export}" Command="{Binding ExportConfigCommand}"
                        Width="60" />
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 左侧：配置列表 -->
            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{DynamicResource Profile_List}" FontSize="14" FontWeight="Bold"
                                   Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" />
                        <Button Grid.Column="1" Content="+" Width="30" Padding="0"
                                Command="{Binding CreateProfileCommand}" Margin="0,0,4,0" />
                        <Button Grid.Column="2" Content="{DynamicResource Delete}" Width="48" Padding="4,2"
                                Command="{Binding DeleteProfileCommand}"
                                CommandParameter="{Binding SelectedProfile}"
                                Background="{DynamicResource ErrorBrush}" />
                    </Grid>

                    <ListBox Grid.Row="1" ItemsSource="{Binding Profiles}"
                             SelectedItem="{Binding SelectedProfile}"
                             Background="{DynamicResource SurfaceBrush}"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             BorderBrush="{DynamicResource BorderBrush}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="4,6">
                                    <TextBlock Text="{Binding Name}" FontSize="13"
                                               Foreground="{DynamicResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0} 个下载地址, {1} 个上传地址">
                                                <Binding Path="DownloadUrls.Count" />
                                                <Binding Path="UploadUrls.Count" />
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- 右侧：选中配置详情 -->
            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}"
                    Visibility="{Binding SelectedProfile, Converter={StaticResource NotNullConverter}, FallbackValue=Collapsed}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Profile_Detail}" FontSize="14" FontWeight="Bold"
                                   Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,12" />

                        <!-- 名称 -->
                        <TextBlock Text="{DynamicResource Name}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
                        <TextBox Text="{Binding SelectedProfile.Name, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,2,0,12"
                                 Background="{DynamicResource CardBrush}"
                                 Foreground="{DynamicResource TextPrimaryBrush}"
                                 BorderBrush="{DynamicResource BorderBrush}" />

                        <!-- 下载地址 -->
                        <TextBlock Text="{DynamicResource DownloadUrls}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,4" />
                        <ItemsControl ItemsSource="{Binding SelectedProfile.DownloadUrls}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource SurfaceBrush}"
                                            CornerRadius="4" Padding="8,4" Margin="0,2">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding}" FontSize="12"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                                       TextTrimming="CharacterEllipsis"
                                                       VerticalAlignment="Center" />
                                             <Button Grid.Column="1" Content="{DynamicResource Delete}"
                                                     Width="40" Padding="2,1" FontSize="11"
                                                     Background="{DynamicResource ErrorBrush}"
                                                    Command="{Binding DataContext.RemoveDownloadUrlCommand,
                                                              RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- 添加下载 URL -->
                        <Grid Margin="0,6,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding NewDownloadUrl, UpdateSourceTrigger=PropertyChanged}"
                                     Background="{DynamicResource CardBrush}"
                                     Foreground="{DynamicResource TextPrimaryBrush}"
                                     BorderBrush="{DynamicResource BorderBrush}" />
                            <Button Grid.Column="1" Content="{DynamicResource Add}" Width="50" Margin="8,0,0,0"
                                    Command="{Binding AddDownloadUrlCommand}"
                                    CommandParameter="{Binding SelectedProfile}" />
                        </Grid>

                        <!-- 上传地址 -->
                        <TextBlock Text="{DynamicResource UploadUrls}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,4" />
                        <ItemsControl ItemsSource="{Binding SelectedProfile.UploadUrls}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource SurfaceBrush}"
                                            CornerRadius="4" Padding="8,4" Margin="0,2">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding}" FontSize="12"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                                       TextTrimming="CharacterEllipsis"
                                                       VerticalAlignment="Center" />
                                             <Button Grid.Column="1" Content="{DynamicResource Delete}"
                                                     Width="40" Padding="2,1" FontSize="11"
                                                     Background="{DynamicResource ErrorBrush}"
                                                    Command="{Binding DataContext.RemoveUploadUrlCommand,
                                                              RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- 添加上传 URL -->
                        <Grid Margin="0,6,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding NewUploadUrl, UpdateSourceTrigger=PropertyChanged}"
                                     Background="{DynamicResource CardBrush}"
                                     Foreground="{DynamicResource TextPrimaryBrush}"
                                     BorderBrush="{DynamicResource BorderBrush}" />
                            <Button Grid.Column="1" Content="{DynamicResource Add}" Width="50" Margin="8,0,0,0"
                                    Command="{Binding AddUploadUrlCommand}"
                                    CommandParameter="{Binding SelectedProfile}" />
                        </Grid>

                        <!-- 保存名称按钮 -->
                        <Button Content="{DynamicResource SaveName}" HorizontalAlignment="Right" Width="80"
                                Command="{Binding SaveProfileNameCommand}"
                                CommandParameter="{Binding SelectedProfile}" />
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
